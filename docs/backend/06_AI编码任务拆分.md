# PCB 评审平台 AI 编码任务拆分

## 任务拆分原则

每个任务按一个业务垂直域交付：领域规则、数据库迁移、应用服务、REST 接口、权限校验、审计/事件影响和自动化测试同时完成。后续任务只能依赖已完成任务公开的接口与数据模型。

## 开发任务清单

| 顺序 | 任务 | 范围 | 完成标准 | 当前状态 |
| --- | --- | --- | --- | --- |
| 1 | 评审任务域 | 草稿创建、提交、列表/详情查询、状态基础、任务级可见性、任务审计 | MyBatis + Flyway 持久化；任务接口与测试通过 | 已完成：已完成创建、提交、详情、含条件与分页的列表、状态基础、`review_task` 持久化、操作审计、提交 Outbox 事件、受限专家按有效人员关系的数据范围校验及任务结束/归档 REST 联调验收。 |
| 2 | 身份与权限域 | Mock 身份、角色矩阵、功能/数据/文件权限、当前节点校验 | 每个写接口具备权限与跨对象越权测试 | 进行中（约 90%）：已完成 Mock 身份、完整功能/文件权限矩阵、EMC 设计文件下载例外、工艺/结构专家基于有效人员关系的数据范围校验、当前节点处理人通用校验、“我的任务”待办动作计算、任务创建/提交/设计文件/人员分配的归属与节点校验及跨对象越权测试；待集中复核余下写接口的统一授权验收。 |
| 3 | 文件域 | 上传会话 Mock、元数据、当前文件替换、下载授权 | 文件上传与下载权限测试通过 | 已完成：本地上传会话 Mock、`review_file` 元数据持久化、同一业务文件键直接替换当前文件、受控下载地址与 EMC 下载例外均已实现并通过自动化测试。 |
| 4 | 评审域：人员与互检 | 人员分配/改派/提交、无意见、固定检查项和额外意见 | 多人全部完成与检查项规则测试通过 | 已完成：已完成人员分配/改派、无意见提交、多人全员提交判断、固定检查项模板变更同步与快照、额外意见入口、任务结束保护、检查项附件、关键 REST 授权及“不合格检查项—同源意见—附件”完整联动测试；模板新增 Excel 批量导入，支持以“评审类型 + 检查项编码”创建或更新且整表原子校验。 |
| 5 | 评审域：意见闭环 | 提出、答复、确认、撤回、历史记录和看板统计 | 意见生命周期与对象归属测试通过 | 已完成：已完成意见主记录、答复/确认历史持久化、提出/答复/确认/撤回/查询接口、固定检查项关联意见的真实归属校验、意见统计看板、关键操作审计、结束任务只读保护及专家提出—设计者关联新版答复—提出人确认 REST 全链路测试；意见列表现同时返回设计者的全部答复轮次及提出人的确认历史。 |
| 6 | 流程与状态编排 | PCB/原理图阶段准入、可执行动作、结束条件和归档 | 非法流转与已结束只读测试通过 | 已完成：已完成 PCB/原理图阶段动作、状态推进、多人完成与意见闭环校验、最新文件校验、检查项快照冻结、流程审计/Outbox、任务结束归档快照、归档只读查询、不可重新打开保护及结束/归档/重复流转 REST 全链路测试；任务和任务内检查项不使用乐观锁版本约束。 |
| 7 | 通知与操作审计 | Outbox、邮件 Mock、重试、通知记录、全量操作审计 | 业务与事件同事务、幂等消费、失败重试测试通过 | 进行中（约 90%）：已完成 Outbox 事件持久化、邮件 Mock、通知发送记录、原子认领、成功/失败状态、最大重试次数限制和发送审计；任务提交/流转、人员分配/改派、文件新版、检查项、模板维护、意见提出/答复/确认均已写入操作审计，关键业务事件均映射为事务内事件，并已完成管理接口消费、发布状态和发送记录的端到端验收；待确定收件人/邮件模板。 |
| 8 | 集成与交付 | OpenAPI、全链路集成测试、Flyway/MySQL 验证、运行说明 | 全量构建、测试、静态检查通过 | 进行中（约 90%）：已具备 H2 MySQL 兼容模式、MySQL 配置模板、OpenAPI/Swagger UI 契约、README 本地/MySQL 启动说明、PCB 全链路 REST 验收、Java 文件头规范自动校验和显式 MySQL 迁移冒烟测试；待本机安装 MySQL 后执行真实实例验证。 |

## 已实施任务记录

### 本轮需求补充（2026-09-18）

- 根包及 Maven GroupId 已由 `com.leapmotor.pcbreview` 迁移为 `com.bms`；生产代码、测试、日志配置和后端架构文档已同步。
- `POST /check-item-templates/import` 已支持 `multipart/form-data` 的 Excel 批量导入。首个工作表表头必须依次包含（顺序可调整）`评审类型`、`检查项编码`、`父级检查项编码`、`检查项名称`、`排序号`、`是否启用`；评审类型支持 `PCB`/`SCHEMATIC`，启用值支持 是/否、true/false 或 1/0。任何行校验失败会使整份导入回滚。
- `GET /tasks/{taskId}/opinions` 的每条意见返回 `replies` 历史数组：每一项包含答复轮次、答复结论、答复内容、答复人、答复时间，并内嵌该轮唯一对应的 `confirmation`（确认结论、意见、确认人、确认时间；尚未确认时为 `null`）。不再仅返回最新答复的扁平字段，前端可完整回显多轮答复与确认历史。

### 专家意见图片与接口语义收敛（2026-09-20，后续方案已替换）

- 旧版 `opinion_attachment`、`OPINION_ATTACHMENT` 及 `attachmentFileIds` 方案已废弃；专家意见和互检单截图统一随 `richText` 字符串传递和回显，不再建立图片附件关联。
- 设计者答复接口统一为 `POST /opinions/{opinionId}/reply`，专家确认接口统一为 `POST /opinions/{opinionId}/confirm`；旧的复数路径保留兼容，避免已发布前端或集成方立即失效。
- 任务详情“专家意见”页支持直接粘贴截图并写入富文本，不提供图片文件登记或下载入口。

### 本地 Mock 初始化账号与角色（2026-09-20）

- 新增 `user_account`、`role_definition`、`user_role` 用户角色基础表，初始化研发部经理王鹏飞、PCB 组长刘满红、六名原理图组长、三名 EMC 专家和两名设计师；邮箱使用 `bms.example.com` 测试域名，全部归属 BMS 硬件部。
- 新增 `GET /mock-users`，返回初始化人员的 ID、姓名、测试邮箱、部门和预设角色；前端顶部本地身份面板可直接切换这些人员。请求头角色仍可手动覆盖，以便权限边界联调。

### PCB 流程工作台视觉重构（进行中，2026-09-20）

- 已按用户提供的流程图将任务详情顶部重构为任务摘要、流程状态和 PCB 十一步流程导航，并扩展页签为任务信息、专家评审、三次设计者答复、工艺评审、结构评审、互检分配、互检单、结束确认和归档记录；工艺、结构文件上传入口收敛至首段设计者答复页。
- 当前任务状态已按业务阶段收敛：工艺与结构评审统一使用“工艺/结构评审”，设计者答复、文件上传和结束准备属于流程动作，不再占用独立任务状态。
- 已根据第二批流程图将互检负责人分配改为读取 `GET /mock-users` 的人员卡片多选，并直接调用既有评审人员分配接口；设计者答复页改为意见看板、状态统计与逐条答复，互检单、三段式归档数据继续使用已实现接口。
- `GET /tasks/{taskId}/archive` 的数据体仅返回 `flowNodes`、`stageFiles`。流程节点仅包含节点时间、节点名称、操作人姓名和流转意见；阶段文件保留文件名、格式、大小、上传人、上传时间、流程节点和受控下载所需信息。不返回专家、工艺、结构或互检评审意见、任务快照、评审人员或邮件记录。本地 Mock 暂用 `用户#ID` 作为显示名，待接入企业用户目录后替换为真实姓名。

### 原理图评审流程工作台适配（进行中，2026-09-20）

- 已将原理图任务详情切换为独立的八步流程：创建、互检分配、互检、设计者答复、硬件专家分配、原理图评审、设计者答复、结束；阶段页签与用户提供的原型一一对应，并保留独立的归档记录页。
- 互检负责人分配会使用 `SCHEMATIC_MUTUAL_CHECK`，硬件专家分配会使用 `SCHEMATIC_HARDWARE_EXPERT`；人员分配成功后，前端调用既有流程接口分别执行 `START_SCHEMATIC_MUTUAL_REVIEW` 或 `START_SCHEMATIC_EXPERT_REVIEW`，不再只是保存人员而没有进入下一阶段。PCB 互检分配同步改为 `PCB_MUTUAL_CHECK` + `START_PCB_MUTUAL_REVIEW`。
- 已新增结束确认检查清单和“确认任务结束”入口，调用既有 `FINISH` 流程动作；后端仍负责校验任务必须已进入结束确认状态、所有处理人已完成、意见已闭环、存在最新设计文件，并在成功后冻结归档。
- 接口复核结论：任务详情使用 `GET /tasks/{taskId}`，人员选择使用当前阶段可分配人员接口，批量互检、意见/答复/确认、流程流转、文件下载和归档接口共同覆盖原型所需的读写数据。原理图状态机目前没有将两次“设计者答复”单独持久化为任务状态，而是通过意见的 `PENDING_REPLY`/`PENDING_CONFIRMATION` 生命周期驱动；页面已按阶段展示。若后续要求这些节点必须作为不可跳过的独立工作流关口，需另行扩展 `TaskStatus` 与流程动作，并补充状态迁移测试。

### 工作台、任务列表与互检单管理页面适配（进行中，2026-09-20）

- 工作台改为统计卡、待处理任务表与流程提醒卡；任务列表和“我的任务”统一任务名称、项目名称、类型、状态、期望完成日期、设计者、评审角色和操作字段的呈现，继续调用既有任务查询与待办接口。
- 创建评审任务页面保持现有字段校验边界和文件多选逻辑，改为从 Mock 用户目录选择设计者、专家/组长；文件由浏览器直接上传公司资源服务，提交仍传用户 ID、冗余姓名及资源服务返回的唯一文件标识，避免页面展示名与服务端权限标识混淆。
- 为支持互检单管理页的真实类别树与操作，`GET /check-item-templates?reviewType=PCB|SCHEMATIC` 返回 `[{ category, items }]`：大类和子项均只含 `id`、`itemName`、`sortNo`，不含 `opinion`、评审类型、内部编码、父级编码、启用状态或版本。删除使用 `DELETE /check-item-templates/<checkItemId>?category=CATEGORY|ITEM`，必须显式声明目标为大类还是小类。大类删除会逻辑停用本评审类型下的全部子项，子项删除只停用该项；类别与记录层级不一致会被拒绝。前端已支持类别树展示、Excel 导入、创建、一次编辑类别及多个子项、逻辑停用。
- 根据互检单管理原型进一步调整：新增类别区固定在页面顶部，直接填写“类别 + 多个检查项”，仅展示业务名称；内部唯一编码在新增时自动生成。下方模板区使用类别卡片、序号、检查项、编辑类别/检查项和删除入口布局；删除按钮仍调用逻辑停用，避免界面文案造成物理删除误解。
- 检查项小类名称编辑收敛为 `PUT /check-item-templates/items/{checkItemId}`：路径仅传检查项 ID，请求体仅传 `itemName`；不要求类别、大类 ID、排序号、内部编码或版本字段。类别编辑仍使用原类别维护接口。
- 修改类别及子项使用 `PUT /check-item-templates`，不再传 `templateId` 或 `reviewType`。`UpdateTemplateRequest` 仅接收 `categoryId`、类别名称和子项列表；子项只传已有项 `itemId`（新增项不传）与 `itemName`。后端根据 `categoryId` 读取类别所属流程，不接收类别或子项的 `enabled`、`version`，并读取保留已有启停状态和版本控制字段。
- 新增检查大项 `POST /check-item-templates` 的 `categoryName` 为必填，`items` 为非必填；可省略或传空数组，后端会只创建大项，后续可再新增对应子项。

### 评审人员白名单（2026-09-22）

- 新增 `POST /reviewer-whitelists`，请求体通过 `roleEmployeeNos` 传递评审角色及多个 `employeeNos`。仅允许硬件（`HARDWARE_EXPERT`）、EMC（`EMC_EXPERT`）、结构（`STRUCTURE_EXPERT`）、工艺（`PROCESS_EXPERT`）和 PCB（`PCB_EXPERT`）评审角色；同一角色不得在请求中重复，单个角色下工号不得重复。前端统一读取 `GET /dictionaries/task-options` 的 `reviewerWhitelistRoles`，获得稳定枚举编码及中文名称。
- 新增 `DELETE /reviewer-whitelists?id={id}` 或 `DELETE /reviewer-whitelists?employeeNo={employeeNo}` 逻辑删除白名单：两者必须二选一；按主键仅删除该条角色—工号映射，按员工工号删除该工号全部启用映射。记录不物理删除，后续新增相同角色和工号时会恢复。
- 任务互检单实例 `task_check_item` 不再保存 `templateItemKey`、`parentItemKey`，改用 `parentId` 表达实例父子关系；查询接口按模板物化类别和子检查项，并在每个检查项的 `opinions` 字段返回该检查项关联的互检意见。
- `reviewer_whitelist` 以一行一个“评审角色 + 员工工号”映射保存，唯一约束保证同一映射不会重复落库。接口重复提交既有映射时幂等忽略，并返回本次新增数量及当前白名单映射。

### 创建/保存任务与评审文件引用一体化提交（已完成，2026-09-21）

- `POST /tasks/save` 和 `POST /tasks/submit` 使用 JSON 任务表单；`files` 为多个已上传文件 UUID 的字符串数组。后端从 `review_file` 的待绑定记录读取文件名、大小、格式、MD5 和资源路径，不接收前端文件元数据或 `businessFileKey`；同一任务重复传入已关联 UUID 会被拒绝。
- 创建页先直连公司资源上传接口，再将返回唯一标识随任务 JSON 发送；PCB 后端不再接收或转发初始文件二进制，也不再需要上传会话、草稿文件关联或单独的初始文件登记接口。
- REST 集成测试验证保存/提交请求携带两个文件 UUID 后，任务进入 PCB 待评审状态，且数据库原样登记两条 `file_id`、资源路径与文件元数据。

### 前端交付：Vue 评审工作台（2026-09-18）

- 已在 `frontend/` 新增 Vue 3 + TypeScript + Vite 工程，复刻原型的紫色侧栏、顶部 Mock 身份选择、卡片、表格、流程步骤和响应式布局。
- 已完成工作台、任务列表、我的任务、创建任务、互检管理，以及任务详情的概览、专家意见、互检检查项、评审人员、阶段文件和归档记录六个标签页；邮件记录、用户管理占位页已移除。
- 已接入统一前缀 `/leapmotor/pcb_review` 下的接口：任务创建/提交/查询、我的任务、意见提出/答复/确认、检查项查询/提交、人员分配/改派、文件上传会话/登记/下载、归档查询与 Excel 模板导入；请求自动携带后端约定的 Mock 用户请求头。
- 已执行 `pnpm run build`，并用本地 Spring Boot H2 服务完成工作台与任务详情页面的实际 API 联调验证。
- 流程推进接口仅要求动作与可选说明，不再要求版本号；任务保存、提交、草稿编辑及任务内互检检查项提交同样不使用版本号，避免将并发控制细节暴露给前端。

### 任务 1：评审任务域（已完成）

- 已交付：评审任务草稿创建、提交、详情与列表接口；任务名称、项目名称、设计者、类型、状态筛选与分页；`review_task` 持久化和重启后恢复；创建/提交审计记录；提交 Outbox 事件；应用、持久化和 REST 自动化测试。
- 已补充：工艺/结构专家仅可见有效的 `task_reviewer` 关联任务；改派后 `CANCELLED` 历史人员不再作为数据范围授权来源。
- 已补充：任务结束、归档读取及结束后拒绝再次流转的 REST 联调已随任务 6 完成。

### 任务 2：身份与权限域（进行中，约 90%）

- 已交付：请求头 Mock 身份；角色与功能权限矩阵；文件上传/下载权限；EMC 专家下载 PCB/原理图文件的用户确认例外；受限专家按有效任务人员关系的数据范围判断；当前节点处理人通用校验；“我的任务”待办动作计算及创建任务的越权测试。
- 已补充：“我的任务”只返回当前需要该用户评审、答复意见、确认意见、分配人员或结束的任务，不会因为用户可见或曾参与而产生待办；多人评审仍由任务流程统一要求全员完成。
- 已补充：人员首次分配和改派均校验任务类型、当前流程阶段与职责的匹配关系；PCB 专家只能由该任务的设计者（或硬件部经理）分配；结束任务拒绝创建上传会话和登记文件。
- 已补充：任务创建和提交仅允许任务设计者（或硬件部经理）执行；仅具设计者角色的用户不能向其他设计者任务上传 PCB/原理图文件，并已覆盖对应越权回归测试。
- 未完成：对全部评审、文件和流程写接口集中补齐统一授权集成验收，并覆盖跨对象越权场景。

### 任务 3：文件与版本域（已完成）

- 已交付：本地上传会话 Mock；`review_file` 文件元数据持久化；同一业务文件键重复上传时直接替换当前文件；受控下载地址；权限校验；文件上传与下载授权自动化测试。
- 已知实现边界：本地阶段只生成 `mock://` 上传和下载地址，不保存文件二进制；后续替换为公司文件 API 适配器。

## 当前执行任务：任务 2（身份与权限域统一授权验收）

当前优先补齐所有写操作的统一权限入口与跨对象越权 REST 验收；随后完成任务 7 的业务事件映射和任务 8 的交付文档、OpenAPI 与 MySQL 实例验证。

### 本轮已完成内容（2026-09-18）

- 已完成任务 4：人员分配/改派、多人全员完成、无意见提交、检查项模板同步与结束冻结、额外意见及检查项附件；Flyway 已推进至 V11。
- 已完成任务 5 和任务 6：意见提出—设计者答复—提出人确认闭环、状态编排、结束条件、归档快照和任务结束后不可重新打开。意见主表冗余保存提出人姓名（`raised_by_name`），意见列表直接返回 `raisedByName`，历史记录不再依赖用户目录回查。
- 已新增 `GET /tasks/my` 和待办动作计算单元测试；本轮修复 Java 17 测试兼容性后，全量 Maven 测试已通过。

### 验收条件

1. 按 PCB/原理图流程权限分配、改派评审人员和互检人员，并写入 `task_reviewer`。
2. 被分配人员可查看关联任务；“我的任务”仅返回当前节点需要其评审、分配、确认或结束的事项。
3. 评审人员可提交意见或“无意见”；同一角色多人时，必须全部提交才算该角色处理完成。
4. 互检支持任务内固定检查项实例和额外意见；已创建未完成任务的检查项随模板变更更新，已结束任务保持快照不变。
5. 自动化测试覆盖改派、无意见、多人全员完成、未分配越权和检查项规则。

## 进度快照（2026-09-18）

当前全量 Maven 自动化测试已通过。任务 1、3、4、5、6 已完成；任务 2 处于统一授权验收阶段，任务 7 已实现业务事件 Outbox 映射、本地 Mock 投递与重试基础，任务 8 已具备 H2 MySQL 兼容配置、OpenAPI 契约和运行说明。

“我的任务”已按业务定义实现：只有当前节点需要当前用户处理的评审、答复、确认、分配和结束动作才会返回；任务可见范围与待办范围彼此独立。

通知收件人和邮件模板仍严格保留 `TBD` 占位，等待业务确认。任务提交/状态流转、人员分配/改派、文件上传及意见提出/答复/确认均已通过统一发布器写入同一业务事务的 Outbox；本地阶段不保存文件二进制，后续替换为公司文件 API 适配器。

任务 7 本轮新增：通知投递不向前端暴露 REST 接口；已由通知应用服务内部消费 Outbox 事件。端到端测试验证事件从待投递状态被原子认领、经本地邮件 Mock 发送后变为 `PUBLISHED`，并可按 Outbox 事件读取对应发送记录。

任务 7 本轮补齐：人员分配/改派/无意见提交、文件上传、检查项提交/附件、检查项模板创建/修改全部追加不可变操作审计记录；关键单元测试已验证审计写入。

任务 8 本轮新增：已集成 Springdoc OpenAPI；服务启动后可从 `/leapmotor/pcb_review/swagger-ui.html` 查看接口文档、从 `/leapmotor/pcb_review/openapi.json` 获取 JSON 契约，并由自动化测试验证契约可访问。根目录 `README.md` 已补齐 H2 Mock、本地身份请求头和 MySQL Profile 的启动步骤；新增 PCB 成功路径 REST 集成测试，覆盖创建、文件登记、提交、专家/互检分配与多人处理、结束、归档和结束后拒绝再次流转。另新增 Java 文件头规范测试，持续校验所有生产 Java 文件在 import 后含 `@author 王涛`、允许日期和中文 `@description`。

### 对外接口统一前缀（2026-09-22）

- 所有 PCB 后端对外接口统一以 `/leapmotor/pcb_review` 为上下文前缀，包括业务 REST 接口、Swagger UI、OpenAPI JSON 与归档文件下载路径；前端默认请求基址和 Vite 开发代理已同步切换。

任务 8 本轮补齐：新增 MySQL Profile 的迁移冒烟测试；仅在设置 `PCB_REVIEW_RUN_MYSQL_TESTS=true` 后连接数据库，验证当前数据库确为 MySQL 且 Flyway 已执行到 V11，正常构建会跳过该测试以避免误连数据库。执行命令已补入 `README.md`。

## 本轮接口与前端契约调整（2026-09-20）

- 已完成任务创建模型扩展（Flyway V12）：任务表冗余设计者姓名与 ID，新增期望完成日期、专家/组长 ID 与姓名、多个评审角色、评审描述；任务详情、任务列表与“我的任务”均返回一致的基础字段和评审角色列表。任务列表设计者筛选改为 `designerName` 模糊查询，不再接收 `designerId`。
- 已新增 `GET /dictionaries/task-options`，向前端提供 PCB 类型（BMU板、BSU板、分流器板、高压板、转接板、储能板、其他）和全部 `ReviewRole` 枚举/中文名称。返回字段为 `data.pcbTypes` 与 `data.reviewRoles`；后者的稳定提交编码依次为 `PCB_EXPERT`、`PROCESS_EXPERT`、`STRUCTURE_EXPERT`、`PCB_MUTUAL_CHECK`、`SCHEMATIC_HARDWARE_EXPERT`、`SCHEMATIC_OTHER_EXPERT`、`SCHEMATIC_MUTUAL_CHECK`。前端创建页已改为调用字典接口、多选评审角色、选择多评审文件、支持保存草稿或直接提交，且不再在浏览器端拦截必填校验，统一显示后端错误。
- 该阶段曾提供草稿文件关联和仅传文件 ID 的提交接口；已在“评审任务接口收敛（2026-09-21）”中废止，当前以保存/提交 multipart 接口为准。
- 已完成意见接口增强：`ReplyView` 返回 `opinionId`；意见支持 `severity`（SERIOUS/GENERAL/MINOR）与 `status` 筛选；汇总接口新增未确认意见及专家、未提交处理结果的评审人员。确认不通过使用 `CONFIRMED_REJECTED` 状态，设计者可再次答复。
- 历史的“无意见提交”与 `/workflow/reviewers` 查询入口已废弃；是否有意见不再是单独的流程动作，候选人员统一由当前节点可分配人员接口返回。
- 检查项模板新增/修改接口改为“检查项大类 + 多个子检查项”一对多请求结构；Excel 导入已兼容旧表头和包含“类别编码、类别名称、检查项编码、检查项名称、排序号”的中文类别表头。尚未收到用户指定的最终 Excel 表头文件，收到后须据实收紧导入校验。
- 已为本轮任务、意见、评审人员、模板和字典 Controller 的接口/请求字段补充中文 Swagger 描述；其余既有 Controller 的 Swagger 字段描述将在接口重构时继续补齐。
- 验证：`pnpm run build` 通过；`mvn test` 通过（59 项测试，1 项 MySQL 实例测试按设计跳过）。

## 流程、文件与意见契约调整（2026-09-21）

- 创建任务字典的 `reviewRoles` 仅返回硬件评审（`HARDWARE_EXPERT`）、EMC评审（`EMC_EXPERT`）、PCB评审（`PCB_EXPERT`）、结构评审（`STRUCTURE_EXPERT`）和工艺评审（`PROCESS_EXPERT`）；PCB/原理图互检职责保留为流程内部职责，不再出现在创建页。
- 创建和保存草稿使用单次 JSON 请求；前端先调用文件上传接口，再把返回的多个 UUID 放入 `files`。后端从 `review_file` 的待绑定记录读取文件元数据，不接收或转发文件二进制。仅任务设计者可编辑 `DRAFT` 任务并追加文件，不需要传任务版本号；同一任务不能重复保存同一 UUID。提交后的任务（包括已产生评审意见的任务）拒绝编辑。提交时自动为 PCB 所选角色或原理图组长创建 `ReviewerView` 记录；PCB 直接进入 `PCB_EXPERT_REVIEWING`，原理图进入 `MUTUAL_CHECK_PENDING_ASSIGNMENT`。
- `WorkflowApplicationService#transition` 已补齐 PCB/原理图“评审—设计者答复—下一阶段”的状态；Swagger 详细说明每个 action、允许的前置状态和完整流程。正式评审人员仅支持首次分配，已移除改派 REST 接口。
- 意见列表新增 `sourceType` 和 `scene`：`REVIEW_WORKSPACE` 仅返回当前登录专家提出的意见，`DESIGNER_REPLY` 返回任务全量意见；两种场景都按提出时间倒序。设计者答复仅接收结论与文字说明，不接收文件或图片。
- 意见列表已改为分页响应：`GET /tasks/{taskId}/opinions` 支持 `pageNo`（从 1 开始）和 `pageSize`（最大 100），返回 `total`、`pageNo`、`pageSize`、`items`；筛选与场景权限在分页前执行，保持提出时间倒序。
- 专家意见的 `content` 支持富文本；当前以“富文本直接存储”方案为准，不再传递 `pastedImages`、图片 URL、意见附件 ID 或文件版本 ID，也不调用资源服务登记专家意见图片。`GET /files/latest?taskId=...&fileCategory=...` 获取当前阶段各业务文件；同一意见的专家内容、设计者最新答复和确认结果均处于同一个扁平模型。
- 文件不再维护业务版本号：同一任务、文件类别和业务文件键重复上传时覆盖当前文件；`FileView`、归档阶段文件和前端均不再返回或展示 `versionNo`。意见与设计者答复均不关联文件版本，数据库迁移 `V16` 已移除 `review_opinion.file_version_id`、`opinion_reply.file_version_id` 及意见乐观锁列。
- 富文本提取意见：Flyway `V17` 为 `review_opinion` 新增 `rich_text_content`、为 `task_check_item` 新增 `opinion_rich_text`。专家评审和互检单均以字符串原样保存文字及内嵌 data URI 图片，查询接口统一通过 `richText` 回显；不增加图片 URL、图片文件或图片上传接口。
- 互检单单条提交和批量提交接收 `result`、`comment`、`richText`（批量另含 `itemId`）：`result` 只允许 `PASS`（合格）、`FAIL`（不合格）和 `NC`；`comment` 是用户填写的文字意见，`FAIL` 或 `NC` 时必填；`richText` 用于补充文字和内嵌图片。接口不接收 `linkedOpinionId`、文件或附件参数，`FAIL` 自动创建或更新同源意见。
- 文件接口现统一由 `FileController` 承载：前端不传资源目录，后端根据环境配置生成含 UUID 的目录并调用公司上传、下载接口；任务文件元数据、权限校验和阶段查询也在该控制器统一完成。
- 文件上传、当前文件查询和授权下载统一收敛到 `FileController`：`POST /files/upload` 接收二进制、可选 `taskId` 和文件类别；有任务时立即登记元数据。`GET /files/latest` 查询当前文件，`POST /files/download` 仅接收 `taskId` 与唯一 `fileId`，后端读取文件类别并完成权限校验后代理公司资源服务下载。
- 文件元数据闭环：`review_file` 记录关联任务、文件类别、业务文件键、原文件名、文件格式、大小、MD5、资源服务返回的资源路径、上传人、上传时间和上传流程节点；`FileView`、阶段文件查询和归档阶段文件均返回这些信息（下载始终由后端受控代理）。Flyway `V18` 新增 `file_format` 字段。
- Excel 导入同一“检查项”单元格时，仅识别单元格首行或换行后的 `1)`、`2)` 形式编号（兼容中文 `）` 和 Excel 转义后的 `\\)`）来拆分子检查项；`1.5mm`、`12.5mm` 等正文中的小数不会作为分隔符。

### 检查项正式导入模板确认（2026-09-20）

- 已根据用户提供的“PCB设计评审记录表--PCB互评”截图调整导入识别规则：自动跳过标题行和 `PCB评审人` 行，在前 20 行内定位真正表头 `序号、评审阶段、类别、检查项、问题描述、提出人、严重等级、处理情况……`。
- 导入仅使用 `类别` 与 `检查项` 生成模板的“检查项大类—子检查项”结构；`评审阶段`、问题描述、提出人、严重等级、处理情况等列属于评审记录填写内容，不会误导入为模板定义。
- 对 Excel 合并单元格导致的空类别行，导入器继承上一条非空类别；类别或检查项文字变更会生成新的稳定内部编码，重复检查项会在导入事务内报错并整体回滚。

### 互检单批量填写与意见闭环（2026-09-20）

- 已依据互检单 UI 新增 `PUT /tasks/{taskId}/check-items/batch`：整张互检单以事务方式提交多个检查项，任何一项业务校验失败都会整体回滚，接口不要求检查项版本号。
- `FAIL` 或 `NC` 项通过 `comment` 提交用户填写的具体文字意见；可通过 `richText` 补充文字和内嵌图片。后端为 `FAIL` 项自动创建或更新 `MUTUAL_CHECK_ITEM` 同源意见，前端无需传意见 ID。
- 检查项查询按 `[{ category, items }]` 返回：`category` 是检查大类（仅含 `id`、`reviewType`、`itemName`、`sortNo`），`items` 是其子检查项且仅含 `id`、`itemName`、`sortNo` 与可空 `opinion`。检查结论、文字意见和富文本仅在 `opinion.result`、`opinion.comment`、`opinion.richText` 返回；`opinion` 不含 `content`、`status`，尚未提交时为 `null`。接口不返回 `parentItemKey`、`version`、附件、图片 URL 或旧的键字段。前端按“序号、类别、检查项、结果、意见与富文本补充”展示。
- 验证：`mvn test` 通过（59 项通过，1 项 MySQL 实例测试按设计跳过）；`frontend` 下 `pnpm run build` 通过。

### 本轮收口补充（2026-09-21）

- 草稿编辑接口 `PUT /tasks/{taskId}` 增加 `submit` 查询参数：`false` 表示仅保存编辑和追加文件，`true` 表示在同一 multipart 请求内保存后提交。前端“编辑任务”页因此同时提供“保存修改”和“保存并提交”，不会产生保存后的草稿无法提交的断点。
- 前端任务文件下载以当前登录请求头调用 `POST /files/download?taskId=...&fileId=...` 取得二进制流；后端从唯一文件记录取得类别并完成权限校验后调用资源服务下载，因此资源服务协议与 PCB 业务规则相互解耦。
- 资源服务上传返回体的 `data` 支持对象形式（如 `data.filePath`）及直接字符串路径，返回的资源路径可原样作为下载接口的 `filePath` 参数。
- 任务创建页调用 `POST /files/upload` 时只传 `fileCategory=TASK_CREATION`，不传 `dirPath`；上传成功后返回 UUID，并直接保存到 `review_file`。此时 `task_id` 为空代表待绑定文件；任务保存/提交时只将多个 UUID 放入 `files`，后端校验上传人和未绑定状态后更新同一记录的任务关联，用于后续下载与归档追溯。
- 正式接口仅保留单数路径 `POST /opinions/{opinionId}/reply`、`POST /opinions/{opinionId}/confirm`，不再保留旧复数路径；历史文档中“保留兼容”的说明以本条为准。

### 设计者重新答复页面（2026-09-21）

- 设计者答复页将 `CONFIRMED_REJECTED` 视为必须处理的“需重新答复”意见，而不再仅作为统计数字展示。卡片会标明上次答复与专家退回原因，并显示“重新提交答复”入口；提交后沿用既有 `POST /opinions/{opinionId}/reply`，生成下一轮答复，直至专家确认通过。

### 专家评审意见列表视觉适配（2026-09-21）

- 意见列表改为参考原型的紧凑行式布局：顶部提供问题等级、状态筛选及数量；每条意见显示严重等级、处理状态、提出人、提出时间、意见内容和右侧问题截图入口。已有设计者答复时在同一行内展开答复及“通过/不通过”操作。
- `OpinionView` 新增 `severity` 与 `createdAt`，使前端不再用固定文案或本地时间模拟问题等级与提出时间；提出意见表单也补充严重/一般/轻微选择。

### 专家评审意见提交区收敛（2026-09-21）

- 专家、工艺、结构评审工作台的提交区仅保留问题截图粘贴区、问题等级和具体评审意见。截图支持直接 Ctrl + V 粘贴多张图片，不显示单独的图片上传控件。
- 前端不再展示或允许选择 `sourceType`；根据当前页签自动传递 `EXPERT_REVIEW`、`PROCESS_REVIEW` 或 `STRUCTURE_REVIEW`，避免专家误选意见来源。
- 意见统计看板仅在三个“设计者答复”页签显示；专家评审页不再显示统计卡片。

### 数据库脚本整理（2026-09-21）

- `src/main/resources/db/migration` 已收敛为两个 Flyway 脚本：`V1__ddl.sql` 包含最终 DDL（表、索引、唯一约束和用户角色外键），`V2__init.sql` 仅包含角色定义、BMS硬件部初始账号及用户角色关联。
- `docs/database/ddl.sql` 与 `docs/database/init.sql` 保留同一份面向人工执行的结构/初始化脚本；全新环境由 Flyway 自动执行 `V1`、`V2`，或按顺序手工执行文档脚本二选一，不应混用。
- 旧的 `V1` 至 `V24` 增量迁移已移除。已有数据库必须保留原迁移历史，或在测试/开发环境清库后重新执行；不能在已有 `flyway_schema_history` 上直接替换为新的两条迁移。

### 评审任务接口收敛（2026-09-21）

- 评审任务模块对外提供 `GET /tasks`（分页查询）、`GET /tasks/{taskId}`（单任务详情）、`GET /tasks/my`（我的待办）、`POST /tasks/save`（保存草稿）和 `POST /tasks/submit`（提交任务）。详情页不再通过分页查询接口反查一条任务。
- 保存和提交均使用 `application/json`：完整任务 JSON 中的 `files` 仅携带资源服务上传后的 UUID 字符串数组，后端从 `review_file` 中读取并绑定元数据，不接受 `businessFileKey`。新建草稿时 `POST /tasks/save` 不传 `taskId`；编辑草稿时通过 `POST /tasks/save?taskId={taskId}` 指定目标任务，JSON 不携带 `id`。PCB 后端不接收初始文件二进制、不新增初始文件上传接口，也不使用任务乐观锁版本号。
- 工艺、结构等已进入流程后的阶段文件上传仍属于文件模块，不属于任务创建/保存接口。

### 专家意见富文本存储（2026-09-21）

- 专家提出意见时，前端仅提交一个富文本 `content` 字符串（正文及粘贴的截图内容均包含在该字符串中）和问题等级、页面自动判定的意见来源等业务字段。
- 删除 `imageUrl`、`pastedImages` 和 `attachmentFileIds` 的意见接口参数及响应字段；后端不再将专家截图上传资源服务或创建意见附件关联。
- Flyway V15 将 `review_opinion.content` 调整为 `LONGTEXT`，以容纳包含截图的富文本字符串，并移除已废弃的 `image_url` 列；全量 DDL 已同步为最终结构。

### 流程入口统一编排（2026-09-22）

- 已移除 `ReviewerAssignmentController`，不再提供独立的评审人员分配 HTTP 入口。`ReviewerAssignmentService` 保留为流程域内部能力，负责人员关系、权限与多人完成状态校验。
- `POST /tasks/{taskId}/workflow/transitions` 按业务动作拆分参数处理：创建任务固定使用 `POST /tasks/save` 或 `/tasks/submit`，互检单/原理图文件二进制固定使用 `POST /files/upload`；流程接口只负责已创建任务的状态推进。分配互检组员、分配原理图专家、设计者带工艺/结构文件开启评审、管理员结束，以及不附加人员或文件的常规节点推进，分别由控制器的独立处理分支转换为应用层命令，避免一个请求混用不相干参数。
- PCB 组长通过 `START_PCB_MUTUAL_REVIEW + assignedRole=PCB_MUTUAL_CHECK + reviewerIds` 分配互检组员并开始互检；原理图组长通过 `START_SCHEMATIC_MUTUAL_REVIEW + assignedRole=SCHEMATIC_MUTUAL_CHECK + reviewerIds` 分配互检组员；硬件开发部经理通过 `START_SCHEMATIC_EXPERT_REVIEW + assignedRole=SCHEMATIC_HARDWARE_EXPERT/SCHEMATIC_OTHER_EXPERT + reviewerIds` 分配专家并开启原理图评审。
- 本段旧流程动作已由文末“流程动作收敛与意见准入”替代：工艺、结构评审使用两个明确动作同次提交，不再使用 `START_PCB_OPTIONAL_REVIEW`。
- 验证覆盖：统一流程接口的 PCB 互检多人分配、无意见提交、受限查询，以及“登记公司工艺图并启动工艺/结构评审”的同事务 REST 场景。

### 当前阶段可分配人员与任务详情接口（2026-09-22）

- 已移除 `GET /tasks/{taskId}/workflow/reviewers`。该接口仅返回既有分配记录，无法满足分配页按当前节点筛选候选人的需求。
- 新增 `GET /tasks/{taskId}/workflow/assignable-reviewers`：流程域依据任务当前状态返回可执行分配的 `reviewRole`，白名单域以对应白名单角色关联启用的 `user_account`，返回可直接提交到 `reviewerIds` 的 `userId`、员工工号、姓名和部门。PCB 互检使用 PCB 白名单；原理图互检使用各专业白名单；原理图专家分配分别返回硬件专家及其他专业专家候选组。非人员分配节点会拒绝查询。
- 新增 `GET /tasks/{taskId}`，通过既有任务可见性规则返回单条 `TaskView`；前端任务详情页改为调用此接口，同时候选人员选择器改为使用当前阶段可分配人员接口，而非读取全部 Mock 用户。

### 任务状态字典（2026-09-23）

- 统一字典接口 `GET /dictionaries/task-options` 的 `taskStatuses` 字段返回全部 `TaskStatus` 枚举及中文名称，供任务列表状态筛选和状态展示使用；不新增单独的状态字典接口。

### 互检模板录入参数收敛（2026-09-22）

- 新增检查项大类及子项时，前端只提交大类名称和按展示顺序排列的子检查项名称；不再传递 `categoryKey`、`itemKey` 或 `sortNo`。
- 后端按所属评审类型和新记录 ID 自动生成稳定内部编码；大类追加至该评审类型的末尾，子项按请求数组顺序自动编号。修改已有类别时保留原有内部编码，并按本次子项数组顺序重排子项。
- 删除类别或子检查项统一使用 `DELETE /check-item-templates/<checkItemId>?category=CATEGORY|ITEM`，传检查项 ID 与目标类别；不再传 `templateId` 或 `version`。删除大类会级联逻辑停用本评审类型下的子项，删除子项仅逻辑停用该项。
- 互检单管理页已改为参考原型的模板树布局：默认只显示类别卡片和子检查项列表，顶部提供“新增类别”和“导入 Excel”；编辑类别、编辑/新增子检查项均在对应类别卡片内展开，不使用遮罩弹窗。

### 专家评审工作台视觉适配（2026-09-22）

- 专家、工艺和结构评审页的工作台改为“最新文件卡—问题截图与评审意见分栏—意见列表”结构；最新文件卡展示当前设计名称与评审类型，并保留下载最新版本文件的操作入口。
- 意见提交区左侧专用于 Ctrl + V 粘贴问题截图，右侧只填写问题等级和具体评审意见；两部分内容仍合并为同一个 `richText` 字符串提交、查询和回显，不增加图片上传接口、意见附件或意见来源表单字段。
- 意见列表继续在同一条记录内展示问题级别、状态、提出人与时间、富文本内容、设计者答复历史及专家确认操作；意见统计看板仍仅在设计者答复页展示。本次为前端视觉与交互布局调整，不涉及接口契约变更。

### 设计者答复工作台视觉适配（2026-09-22）

- 设计者答复页以四项横向意见看板展示待答复、待确认、确认不通过和确认通过数量，并保留待处理提示。
- 意见列表保持按提出时间倒序；单条意见以问题等级、状态、提出人、时间、富文本截图和意见正文为主内容，设计者仅需选择答复结论并提交。退回意见仍可在同一模型中查看历史答复与专家确认结论。
- 页面不新增答复以外的“上传最新版本文件”入口；流程推进继续受既有逐项答复和后端准入限制约束。本次不新增接口，也不改变答复参数。

### PCB 工艺/结构图上传视觉适配（2026-09-22）

- PCB 的两个并列压缩文件选择卡（结构图、工艺图）放在专家评审后的第一段“设计者答复”页，页脚提供“上传并开启工艺/结构评审”作为当前流程的唯一主操作；不再提供独立“上传工艺/结构图”页签。
- 提交沿用既有资源文件上传与任务阶段文件登记能力。前端上传结构图、工艺图后，同次传入 `START_PCB_STRUCTURE_REVIEW`、`START_PCB_PROCESS_REVIEW`；阶段文件使用 `PROCESS_REVIEW` 场景，以 `fileKind=STRUCTURE|PROCESS` 生成不同内部关联键，不暴露或要求 `businessFileKey`，避免两个文件彼此覆盖。
- 前序意见未全部确认通过时仍由后端拒绝进入工艺/结构评审，不新增专用上传或推进接口。

### PCB 阶段文件入口合并（2026-09-23）

- PCB 十一步流程导航去除“上传工艺/结构图”独立页签，避免设计者在答复完成后再切换到另一个页面提交同一阶段动作。
- 原“上传最新版本文件”和“提交流程”顶部按钮均已移除；仅在 PCB 的首段“设计者答复”工作台展示结构图、工艺图双文件上传区域。上传成功后仍复用文件域的两次 `uploadTaskFile` 调用，并以同一组 `START_PCB_STRUCTURE_REVIEW`、`START_PCB_PROCESS_REVIEW` 动作推进流程。
- 原理图评审及 PCB 后续的设计者答复页不展示该双文件上传区，不改变其既有流程入口。

### 菜单与归档展示收敛（2026-09-23）

- 前端侧边栏及路由移除“邮件记录”“用户管理”两个占位入口和页面；本地 Mock 身份选择保留，仅用于开发联调与当前用户权限模拟，不属于用户管理功能。
- 任务归档页面和 `GET /tasks/{taskId}/archive` 不再展示或返回邮件投递记录，仅保留流程节点和阶段文件。通知 Outbox 仍是后端内部可靠投递机制，不向前端暴露邮件记录。

### 评审白名单管理（2026-09-23）

- 新增前端“评审白名单管理”菜单与页面，提供硬件评审、EMC评审、结构评审、工艺评审、PCB评审五个角色分组；每组可从初始化人员目录勾选多名人员后批量保存。
- `GET /mock-users` 增加 `employeeNo`，白名单页直接使用初始化人员的姓名、工号、部门和预设角色展示候选人，不引入用户管理页面。
- `GET /reviewer-whitelists` 返回当前启用的角色—员工工号映射及主键；页面新增仍复用 `POST /reviewer-whitelists`，删除使用既有 `DELETE /reviewer-whitelists?id={id}` 精确逻辑删除单条映射。

### 文件类别收敛（2026-09-22）

- `FileCategory` 统一为六个业务类别：`TASK_CREATION`（任务创建）、`PCB_REVIEW`（PCB评审）、`SCHEMATIC_REVIEW`（原理图评审）、`MUTUAL_CHECK_REVIEW`（互检单评审）、`PROCESS_REVIEW`（工艺评审）、`STRUCTURE_REVIEW`（结构评审）。旧的 `PCB_SCHEMATIC`、`PROCESS`、`STRUCTURE`、`MUTUAL_CHECK_ATTACHMENT` 和 `OPINION_ATTACHMENT` 不再作为文件类别。
- 上传场景仍保持创建、工艺、原理图和互检单四类。工艺/结构图上传统一使用允许的 `PROCESS_REVIEW` 场景，后端根据 `fileKind=PROCESS|STRUCTURE` 分别落为 `PROCESS_REVIEW`、`STRUCTURE_REVIEW` 两种文件类别，并生成独立内部关联键。
- 阶段文件查询、文件下载权限、归档阶段名称及前端“下载最新版本文件”均已同步改用新的文件类别枚举。

### 统一文件接口与资源目录配置（2026-09-22）

- `FileController` 已合并原 `TaskFileController` 的任务文件登记、当前文件查询与受控下载能力，后者不再暴露 REST 接口。
- `POST /files/upload` 不再接收 `dirPath`；前端传 `file`、可选 `taskId` 和必填 `fileCategory`。后端按环境变量 `RESOURCE_SERVICE_DIR_PATH` 读取目录，测试默认 `/user/bms_platform/pcb_review/test1`，`prod` 默认 `/user/bms_platform/pcb_review/pro`，并在目录末尾追加 UUID 后调用公司上传接口。上传元数据统一写入 `review_file`：没有 `taskId` 时仅允许 `TASK_CREATION` 且保留空任务关联；有 `taskId` 时立即登记任务、类别、大小、格式、`file_id`、资源路径、上传时间和当前流程节点。

### 文件表合并（2026-09-23）

- 删除 `pending_file_upload`：它原本仅保存任务创建前上传、尚未关联 `task_id` 的初始文件，和 `review_file` 的元数据字段重复。
- `review_file.task_id` 调整为可空；为空即“待绑定文件”，任务保存或提交后将同一行绑定到任务，避免复制或重复存储文件元数据。
- 原 `company_file_id` 统一改为 `file_id`，用于保存上传接口返回给前端的唯一 UUID；新增/保留 `resource_path`，仅供后端调用公司下载接口，不作为前端文件标识。

### 任务列表评审角色中文展示（2026-09-23）

- 任务列表请求任务分页数据时并行读取统一字典接口，以 `reviewRoles` 中的 `code -> name` 映射展示评审角色中文名称；任务接口和提交参数仍保持枚举 code，避免显示文案影响后端契约。

### 互检模板逻辑删除调用修正（2026-09-23）

- 互检单管理页删除大类或小类统一调用 `DELETE /check-item-templates/{checkItemId}?category=CATEGORY|ITEM`，该 Controller 入口显式绑定路径与查询参数后调用 `CheckItemTemplateApplicationService#disable`；大类使用 `CATEGORY`，小类使用 `ITEM`。
- 前端删除按钮使用显式 `type="button"`，避免小类编辑表单中的删除点击同时触发表单保存请求。

### 设计者答复页操作边界（2026-09-23）

- 专家评审、工艺评审和结构评审页仅展示评审意见及专家确认操作，不展示设计者的“提交答复/重新提交答复”按钮。
- 设计者答复页才展示答复控件；对于 `CONFIRMED_REJECTED` 的意见，重新答复说明改为可见的多行文本框并要求填写后方可提交。
- “通过 / 不通过”仅在专家评审意见页向提出该意见的专家展示；设计者答复页不展示确认按钮。
- 设计者答复页的意见看板读取独立的意见汇总接口，不再从当前经过分页或条件筛选的意见列表计算，因此筛选列表不会改变看板统计。
- 意见看板的四个状态卡可直接点击筛选下方意见列表：待答复、待确认、确认不通过和确认通过分别写入相应的列表状态筛选条件，重置至第一页并滚动到列表；看板统计本身保持独立，不随筛选结果变化。
- 设计者答复页在对应阶段的所有意见均确认通过后提供“流程推进”区：PCB 工艺/结构评审后的答复上传最新 PCB 文件并进入互检单分配，PCB 互检答复提交后准备结束；原理图互检答复和专家答复分别上传最新原理图文件后进入专家分配、准备结束。按钮在存在待答复、待确认或退回意见时禁用，实际准入仍以工作流后端校验为准。
- PCB 第二次“设计者答复”页的意见列表与意见看板仅查询 `PROCESS_REVIEW`、`STRUCTURE_REVIEW` 两类来源，不混入专家评审或互检意见；意见列表和汇总接口新增可选 `sourceTypes`（逗号分隔）参数，以相同来源集合完成分页、状态筛选和统计。
- 公司资源服务前缀由 `RESOURCE_SERVICE_API_PREFIX` 配置，默认 `http://10.195.135.149:30860/leapmotor/pm/resource`；上传、下载分别调用该前缀下的 `/upload`、`/download`。不再使用 Mock 地址、伪造资源路径或空下载内容；资源服务未配置或调用失败时明确返回外部服务异常。 `POST /files/download` 必传 `taskId`、唯一 `fileId`；后端根据文件记录取得类别并进行权限校验，再代理资源服务二进制响应。

### 公司资源上传成功契约（2026-09-23）

- 公司上传接口成功时不返回文件路径或文件 ID，响应条件为 HTTP 状态码 `200`、JSON 响应的 `code=200`，且 `success` 为布尔值 `true`。文件模块不再尝试读取 `data`、`filePath`、`path`、`resourcePath` 或 `id`。
- PCB 在调用资源服务前生成 UUID，并将 UUID 追加到已配置的 `dirPath`。上传成功后，PCB 使用该 UUID 作为对前端返回的文件唯一标识，并保存同一 UUID 目录作为资源路径；`success=false`、空响应体或非 200 响应均视为失败，不会落库文件元数据。

### 流程动作收敛与意见准入（2026-09-23）

- `WorkflowAction` 仅保留十个业务动作：`CREATE`、`START_PCB_STRUCTURE_REVIEW`、`START_PCB_PROCESS_REVIEW`、`START_PCB_MATUAL_ASSIGNMENT`、`START_PCB_MATUAL_REVIEW`、`START_SCHEMATIC_MATUAL_REVIEW`、`START_SCHEMATIC_EXPERT_ASSIGNMENT`、`START_SCHEMATIC_EXPERT_REVIEW`、`PREPARE_FINISH`、`FINISH`。`CREATE` 是任务提交职责：`POST /tasks/submit` 创建 PCB 任务后直接进入专家评审，创建原理图任务后直接进入互检单分配；`transition` 收到 `CREATE` 会明确拒绝，避免将创建与既有任务的状态推进混用。
- PCB 流程顺序为：创建后专家评审；设计者上传工艺、结构文件后，同次以 `START_PCB_STRUCTURE_REVIEW`、`START_PCB_PROCESS_REVIEW` 开启结构、工艺评审；全部前序意见闭环后，设计者上传最新 PCB 文件并以 `START_PCB_MATUAL_ASSIGNMENT` 进入互检单分配；PCB 组长以 `START_PCB_MATUAL_REVIEW` 携带 `PCB_MUTUAL_CHECK` 及 `reviewerIds` 分配并开启互检；互检意见闭环后由设计者执行 `PREPARE_FINISH`，最后由 PCB 组长 `FINISH`。
- 原理图流程顺序为：创建后互检单分配；组长以 `START_SCHEMATIC_MATUAL_REVIEW` 携带 `SCHEMATIC_MUTUAL_CHECK` 及 `reviewerIds` 开启互检；互检闭环后由设计者上传最新原理图文件并使用 `START_SCHEMATIC_EXPERT_ASSIGNMENT` 进入硬件专家分配；硬件开发部经理以 `START_SCHEMATIC_EXPERT_REVIEW` 携带原理图专家及 `reviewerIds` 开启评审；专家意见闭环后设计者上传最新原理图文件并 `PREPARE_FINISH`，最后由原理图组长 `FINISH`。
- 准入条件按意见来源精确校验：开启 PCB 工艺或结构评审前仅要求 `EXPERT_REVIEW` 全部确认通过（或撤回）；开启 PCB 互检单分配前要求专家、工艺和结构意见全部确认通过（或撤回）；开启原理图专家分配前要求互检单意见全部确认通过（或撤回）；结束任一任务前要求该任务的全部意见确认通过（或撤回）。不再将“评审人无意见提交”或“所有处理人已提交”作为额外的流程动作或硬性流转条件。
- `PREPARE_FINISH` 也按流程类型校验：PCB 必须全部互检单意见通过，原理图必须全部原理图专家评审意见通过；该动作仅任务设计者可执行。进入待结束节点后，`FINISH` 由 PCB/原理图组长执行，并再统一校验该任务全部历史意见均已确认通过或撤回。
- 工艺、结构动作要求分别已有对应阶段文件；文件可先由文件接口上传，也可随该次 transition 登记。控制器 Swagger 已列明每个动作的可传参数：互检/专家评审动作接收分配人员，工艺/结构动作接收阶段文件，`PREPARE_FINISH` 与 `FINISH` 不接收额外人员或文件参数。
- 任务详情页同步改用新动作名称：上传结构/工艺文件后分别调用对应启动动作；PCB 互检页先显示“开启互检单分配”，原理图硬件专家页先显示“开启硬件专家分配”，结束页按当前节点显示“准备结束”或“确认任务结束”。
- `transition` 请求使用 `actions` 数组而非单个 `action`：PCB 设计者同时开启工艺、结构评审时传 `actions=["START_PCB_STRUCTURE_REVIEW","START_PCB_PROCESS_REVIEW"]`，服务在同一事务内登记阶段文件并依次完成两个动作；其他场景 `actions` 必须只含一个动作。设计者在开启 PCB 互检单分配、原理图硬件专家分配或准备结束前上传的最新版本文件，仍先调用文件域 `POST /files/upload`（携带 `taskId` 和相应文件类别）登记文件信息，再提交对应单动作，保持资源服务调用和流程域解耦。
- 阶段文件也可随流程请求通过 `stageFiles` 登记：工艺/结构评审使用 `PROCESS_REVIEW`（以 `fileKind=PROCESS|STRUCTURE` 区分），PCB 互检单分配使用 `PCB_REVIEW`，原理图专家分配与原理图准备结束使用 `SCHEMATIC_REVIEW`。服务会在推进前确认当前动作所需的最新文件已保存，防止只推进状态却遗漏设计文件。

### 任务状态收敛与中文展示（2026-09-23）

- `TaskStatus` 仅保留八个业务状态，并在枚举中维护中文 `displayName`：`DRAFT`（草稿）、`PCB_EXPERT_REVIEWING`（专家评审）、`PCB_PROCESS_STRUCTURE_REVIEWING`（工艺/结构评审）、`MUTUAL_CHECK_PENDING_ASSIGNMENT`（互检单待分配）、`MUTUAL_CHECK_REVIEWING`（互检单评审）、`SCHEMATIC_PENDING_HARDWARE_EXPERT_ASSIGNMENT`（待分配硬件专家）、`SCHEMATIC_REVIEWING`（原理图评审）、`FINISHED`（结束）。
- PCB 与原理图共用互检单待分配、互检单评审、草稿和结束状态；前端状态标签、任务筛选、流程步骤、归档节点名称和字典接口均显示中文名称，不再展示旧技术状态。
- `PREPARE_FINISH` 是设计者的流程确认动作，只记录流程历史并保持当前评审状态；组长执行 `FINISH` 后才将任务改为“结束”并归档，因此不再保留“待结束确认”任务状态。

### 互检模板和附件表收敛（2026-09-23）

- 删除 `check_item_attachment` 与 `opinion_attachment` 表及其 MyBatis Mapper。互检单、专家意见中的截图和图文继续只保存在各自的 `richText` 字符串，不再有任何附件关联表或附件查询逻辑。
- `check_item_template` 删除 `item_key` 与不能脱离它使用的 `parent_item_key`，统一使用 `id` 和可空 `parent_id` 表达类别与子项关系；模板树查询、任务检查项物化、创建、编辑、删除和 Excel 导入均改为按 ID 关联。
- Excel 导入仅接收“类别、检查项”及可选“评审类型”列；不保存检查项编码。检查项单元格中行首的 `1)`、`2)`、`3)` 等编号仍会拆分为独立子项。
- `check_item_template.sort_no` 用于界面展示序号，序号从 `1` 开始：同一评审类型的所有大类共享一组连续序号；每个大类下的子项各自从 `1` 重新编号。新增、编辑和 Excel 导入均由后端按请求/文件展示顺序赋值，前端不传该字段。
- `CheckItemTemplateController` 的类别更新与子项名称更新已合并为 `PUT /check-item-templates`。请求统一传 `itemId`、`itemName`；当 `itemId` 指向大类时可选传 `items` 维护该类子项，当其指向小类时不得传 `items`。

### 评审白名单展示字段（2026-09-23）

- `GET /reviewer-whitelists` 联表读取初始化人员目录，白名单列表每条映射返回 `id`、`displayName`（姓名）、`employeeNo`（工号）、`email`（邮箱）、`mobile`（手机号）、`reviewRole`（评审角色）和 `createdAt`（创建时间）。前端按上述字段以表格展示，删除仍只传白名单 `id`。
- 初始化人员表 `user_account` 增加 `mobile` 字段，初始化用户查询也一并返回手机号，确保新增白名单候选人与已保存白名单展示使用一致的人员信息来源。
